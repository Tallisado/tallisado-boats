@setvar! hasRegs 0
if findtype 0xF7A
    if findtype 0xF7B
        if findtype 0xF86
            @setvar! hasRegs 1
        endif
    endif
endif
wait 2000
if hasRegs = 1
    say '[recall room'
else
    say '[recallcharge room'
endif
